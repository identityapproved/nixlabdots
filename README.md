# nixlabdots

NixOS configuration for my home lab machine.

Includes:
- SSH hardening
- rootless Docker
- Zsh environment
- declarative system configuration

System configuration lives in `/etc/nixos` and is version-controlled for reproducibility.
